[package]
name = "chaos"
version = "0.1.0"
edition.workspace = true

[dependencies]

[dev-dependencies]
criterion = "0.8.2"
safety = { path = "../safety" }

[[bench]]
name = "chaos_benches"
harness = false

[package.metadata.aiot]
framework = "aiot"
framework_version = "1"
sdk = "1"
