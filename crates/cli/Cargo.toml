[package]
name = "cli"
version = "0.1.0"
edition.workspace = true

[dependencies]
clap.workspace = true
runtime = { path = "../runtime" }

[package.metadata.aiot]
framework = "aiot"
framework_version = "1"
sdk = "1"
