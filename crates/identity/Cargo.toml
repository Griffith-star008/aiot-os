[package]
name = "identity"
version = "0.1.0"
edition = "2021"
description = "Identity and Access Management"

[dependencies]
aiot_core = { path = "../core" }

[package.metadata.aiot]
framework = "aiot"
framework_version = "1"
sdk = "1"
