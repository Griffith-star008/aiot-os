[package]
name = "ota_update"
version = "0.1.0"
edition = "2021"
description = "OTA Update Framework"

[dependencies]
aiot_core = { path = "../core" }

[package.metadata.aiot]
framework = "aiot"
framework_version = "1"
sdk = "1"
