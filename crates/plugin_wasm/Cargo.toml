[package]
name = "plugin_wasm"
version = "0.1.0"
edition = "2021"
description = "WASM Plugin Integration"

[dependencies]
aiot_core = { path = "../core" }

[package.metadata.aiot]
framework = "aiot"
framework_version = "1"
sdk = "1"
