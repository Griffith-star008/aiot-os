[package]
name = "rag_framework"
version = "0.1.0"
edition = "2021"
description = "RAG Framework (Chunk, Embed, Retrieve)"

[dependencies]
aiot_core = { path = "../core" }

[package.metadata.aiot]
framework = "aiot"
framework_version = "1"
sdk = "1"
