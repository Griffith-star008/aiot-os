[package]
name = "safety"
version = "0.1.0"
edition.workspace = true

[dependencies]
aiot_core = { path = "../core" }
thermal = { path = "../thermal" }

[package.metadata.aiot]
framework = "aiot"
framework_version = "1"
sdk = "1"
