[package]
name = "tenant"
version = "0.1.0"
edition = "2021"
description = "Multi-Tenant Management"

[dependencies]
aiot_core = { path = "../core" }

[package.metadata.aiot]
framework = "aiot"
framework_version = "1"
sdk = "1"
