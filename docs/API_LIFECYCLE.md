# API Lifecycle

## Review Process
All API changes must go through the API Review Board before being merged into `api/`.

## Breaking Change Process
Breaking changes in `Stable` APIs are strictly prohibited in Minor and Patch versions.
They must be queued for the next Major release.

## Migration Policy
A migration guide must be provided for any API transitioning to `deprecated/`.
