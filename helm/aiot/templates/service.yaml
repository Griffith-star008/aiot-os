apiVersion: v1
kind: Service
metadata:
  name: {{ include "aiot.fullname" . }}
  labels:
    {{- include "aiot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aiot.selectorLabels" . | nindent 4 }}
